#pragma once

// Some super obvious functions
bool IsAlpha(char c);
bool IsHexChar(char c);
bool IsHexadecimal(char c);
bool IsDigit(char c);
bool IsKeywordCharacter(char c);

double HexToNumber(char c);
double CharToDigit(char c);
